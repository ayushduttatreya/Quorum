{
  "name": "@quorum/benchmarks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bench:lock": "tsx src/lock-latency.ts",
    "bench:quorum": "tsx src/quorum-commit.ts",
    "bench:replication": "tsx src/replication-lag.ts",
    "bench:all": "npm run bench:lock && npm run bench:quorum && npm run bench:replication"
  },
  "dependencies": { "@quorum/sdk": "*", "@quorum/types": "*" },
  "devDependencies": { "tsx": "^4.0.0", "typescript": "*" }
}

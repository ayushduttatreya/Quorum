{
  "name": "quorum",
  "private": true,
  "workspaces": ["packages/*", "apps/*", "benchmarks"],
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "prettier": "^3.0.0"
  }
}
